@*
 * SPDX-FileCopyrightText: Copyright Corsinvest Srl
 * SPDX-License-Identifier: AGPL-3.0-only
*@
@inherits AdminComponentBase

<RadzenButton Variant="Variant.Text"
              ButtonStyle="ButtonStyle.Light"
              class="rz-button-icon-only"
              Click="ShowTasksMenu"
              title="@L["Active Tasks"]"
              aria-label="@L["Active Tasks"]"
              Style="position: relative;">

    <RadzenIcon Icon="@(BadgeText is not null ? "notifications" : "notifications_none")" />

    @if (BadgeText is not null)
    {
        <RadzenBadge BadgeStyle="@BadgeStyle"
                     Text="@BadgeText"
                     IsPill="true"
                     Style="position: absolute; top: -4px; right: -6px; font-size: 0.6rem; min-width: 16px; height: 16px; padding: 0 4px;" />
    }
</RadzenButton>

    @code{

    private RenderFragment RenderPanel =>@<ActiveTasksPanel/>;
}
