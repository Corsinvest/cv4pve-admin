@inherits AdminComponentBase

@* <RadzenStack Orientation="Orientation.Vertical">
    <RadzenStack Orientation="Orientation.Horizontal" Gap="0">
        <RadzenButton Variant="Variant.Text"
                      Size="ButtonSize.Small"
                      Text="@L["New"]"
                      Icon="add"
                      Click="AddAsync"
                      ButtonStyle="ButtonStyle.Primary" />

        <RadzenButton Variant="Variant.Text"
                      Size="ButtonSize.Small"
                      Text="@L["Delete"]" Icon="delete"
                      Click="DeleteAsync"
                      Disabled="!SelectedItems.Any()"
                      ButtonStyle="ButtonStyle.Danger" />

        <RadzenButton Variant="Variant.Text"
                      Size="ButtonSize.Small"
                      Text="@L["Test"]"
                      Click="TestAsync"
                      Icon="play_arrow"
                      Style="--rz-icon-fill: 1;"
                      Disabled="!SelectedItems.Any()" />

        <RadzenButton Variant="Variant.Text"
                      Size="ButtonSize.Small"
                      Text="@L["Refresh"]"
                      Icon="refresh" />

        <RadzenButton Variant="Variant.Text"
                      Size="ButtonSize.Small"
                      Text="@L["Export"]"
                      Icon="download" />
    </RadzenStack>

    <RadzenDataGrid TItem="JobHook"
                    Data="@Items"
                    AllowFiltering="true"
                    AllowColumnResize="true"
                    AllowAlternatingRows="false"
                    AllowSorting="true"
                    Style="height: calc(100vh - 260px);"
                    PagerHorizontalAlign="HorizontalAlign.Left"
                    FilterPopupRenderMode="PopupRenderMode.OnDemand"
                    FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive"
                    CellClick="CellClick"
                    RowSelect="RowSelect"
                    @bind-Value="@SelectedItems">
        <Columns>
            <RadzenDataGridColumn Property="@nameof(JobHook.Description)" Title="@L["Description"]">
                <Template>
                </Template>
            </RadzenDataGridColumn>

            <RadzenDataGridColumn Property="@nameof(JobHook.Order)" Title="@L["Order"]" SortOrder="SortOrder.Ascending" Sortable="true" />

            <BoolIconColumn Property="@nameof(JobHook.Enabled)" Title="@L["Enabled"]" IconFalse="" />

            <RadzenDataGridColumn Property="@nameof(JobHook.Phase)" Title="@L["Phase"]" />
        </Columns>
    </RadzenDataGrid>
</RadzenStack>
 *@
